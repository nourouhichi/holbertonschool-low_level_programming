#include "holberton.h"
/**
 * puts2 - check the code for Holberton School students.
 * @str : char
 * Return: Always 0.
 */
void puts2(char *str)
{
int i x;
for (i = 0; str[i] != '\0'; i++)
;
for (x = 0; x <= i - 1; c = c + )2
_putchar(str[x]);
_putchar('\n');
}
